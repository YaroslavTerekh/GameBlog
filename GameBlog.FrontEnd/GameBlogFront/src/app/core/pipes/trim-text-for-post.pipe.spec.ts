import { TrimTextForPostPipe } from './trim-text-for-post.pipe';

describe('TrimTextForPostPipe', () => {
  it('create an instance', () => {
    const pipe = new TrimTextForPostPipe();
    expect(pipe).toBeTruthy();
  });
});
